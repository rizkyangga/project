alert("Selamat datang di Tim I47");
const firstName = prompt("Siapa nama depanmu?");

const user = {
   name: {
       first: firstName,
   },
};

if (user.kabar === "baik") {
   alert("Semoga hari mu menyenangkan " + user.name.first + "!");
} else {
alert("Semoga hari mu menyenangkan " + user.name.first + "!");
}
